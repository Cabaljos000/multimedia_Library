<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Multimedia Library" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "css-zero/reset", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "css-zero/variables", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "css-zero/utilities", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 text-gray-900 font-sans">
    <header class="bg-blue-600 text-white py-4 shadow-md">
      <div class="container mx-auto flex justify-between items-center px-4">
        <h1 class="text-3xl font-bold">Multimedia Library</h1>
        <nav class="space-x-4">
          <%= link_to "Home", root_path, class: "px-4 py-2 bg-blue-500 hover:bg-blue-700 text-white rounded-md transition" %>
          <%= link_to "Movies", movies_path, class: "px-4 py-2 bg-gray-500 hover:bg-gray-700 text-white rounded-md transition" %>
          <%= link_to "Books", books_path, class: "px-4 py-2 bg-gray-500 hover:bg-gray-700 text-white rounded-md transition" %>
          <%= link_to "Music", musics_path, class: "px-4 py-2 bg-gray-500 hover:bg-gray-700 text-white rounded-md transition" %>
        </nav>
        
        <% if user_signed_in? %>
          <div>
            <p class="text-sm">Welcome, <%= current_user.email %>!</p>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-white underline text-sm" %>
          </div>
        <% else %>
          <div class="text-sm">
            <%= link_to "Login", new_user_session_path, class: "underline" %> |
            <%= link_to "Sign Up", new_user_registration_path, class: "underline" %>
          </div>
        <% end %>
      </div>
    </header>

    <main class="container mx-auto py-6 px-4">
      <% if flash[:notice] %>
        <div class="mb-4 p-4 bg-green-200 text-green-800 border-l-4 border-green-600 rounded-md">
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert] %>
        <div class="mb-4 p-4 bg-red-200 text-red-800 border-l-4 border-red-600 rounded-md">
          <%= flash[:alert] %>
        </div>
      <% elsif flash[:error] %>
        <div class="mb-4 p-4 bg-yellow-200 text-yellow-800 border-l-4 border-yellow-600 rounded-md">
          <%= flash[:error] %>
        </div>
      <% end %>
      
      <div class="bg-white p-6 rounded-lg shadow-md">
        <%= yield %>
      </div>
    </main>
  </body>
</html>