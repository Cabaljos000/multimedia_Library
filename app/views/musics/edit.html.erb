<% content_for :title, "Editing music" %>

<h1 class="font-bold text-4xl mbe-4">Editing music</h1>

<%= render "form", model: @music %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Show this music", @music, class: "btn" %>
  <%= link_to "Back to musics", musics_path, class: "btn" %>
</div>

